import React from "react";
import classNames from "classnames/bind";
import styles from "./DecileBacktest.module.scss";

const cx = classNames.bind(styles);

export default function DecileBacktest() {
  return <div>DecileBacktest</div>;
}
